# CUE API endpoints
# CRUD: explotaciones, unidades, líneas, recintos, tratamientos
# POST /validate — motor de reglas
# POST /submit — firma + envío a administración
# GET /status/{envio_id} — estado del envío
